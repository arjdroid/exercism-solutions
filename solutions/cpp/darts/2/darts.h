#pragma once
#include <string.h>
#include <float.h>
#include <cmath>

namespace darts {

// concentric coordinates (center 0,0) input, score output
int score(float x, float y);

}  // namespace darts
